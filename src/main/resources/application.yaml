spring:
  application:
    name: backend
  datasource:
    url:
    username:
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  ai:
    openai:
      speech:
        enabled: false
  profiles:
    active: local


env:
  open-data-key:

  clinic-code-url:

  hospital-list-url:
